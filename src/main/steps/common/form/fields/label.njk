{% from "govuk/components/label/macro.njk" import govukLabel %}
{{ govukLabel({
    id: field.id if field.id else fieldName,
    classes: field.classes if field.classes else "govuk-label",
    name: fieldName, 
    type: field.type,
    text: getContent(field.label)
}) }}
