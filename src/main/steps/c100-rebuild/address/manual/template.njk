{% extends "common/template.njk" %}

{% block page_content  %}
  <h1 class="govuk-heading-l">{{title}}</h1>

  {% if partyType === 'respondent-details' %}
    <div class="govuk-inset-text">{{subtitle}}</div>
  {% endif %}
{% endblock %}