{% extends "common/template.njk" %}

{% block page_content %}
    <h1 class="govuk-heading-xl">{{title}}</h1>
    <p class="govuk-body">{{ refugeExplanation }}</p>
    <p class="govuk-body">{{ refugeShelter | safe }}</p>
{% endblock %}