{% extends "common/template.njk" %}

{% block page_content %}
  <span class="govuk-caption-l">{{ caption }}</span>
  {% if title %}
    <h1 class="govuk-heading-l">{{ title }}</h1>
  {% endif %}

  <p class="govuk-body-m">{{ reasonCantBeInformed }}<p>
  <p class="govuk-body-m">{{ needToAsk }}<p>
  
{% endblock %}